# VidMind

`vidmind` starts the local VidMind browser service from npx.

```bash
npx vidmind
npx vidmind start --port 3839
npx vidmind doctor
```

Python 3.12 is required. On first run, VidMind creates a managed Python virtual environment under the user's local app data directory.